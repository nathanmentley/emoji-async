{
    "name": "emojiasync",
    "version": "0.0.1",
    "license": "MIT",
    "scripts": {
        "install": "emojicodec -p emojiasync ./🔧/🏁.🍇 -o ./testPackages/emojiasync/libemojiasync.a && cp ./testPackages/emojiasync/libemojiasync.a ./ && cp ./testPackages/emojiasync/🏛 ./ ",
        "build": "yarn run install",
        "clean": "rm -f testPackages/emojiasync/* && rm -f bin/* && rm -f libemojiasync.a && rm -f 🏛",
        "test": "yarn clean && yarn build && emojicodec -S ./testPackages ./🧪/🏁.🍇 -o ./bin/test && ./bin/test",
        "example": "yarn clean && yarn build && emojicodec -S ./testPackages ./❓/🏁.🍇 -o ./bin/example && ./bin/example"
    },
    "dependencies": {}
}